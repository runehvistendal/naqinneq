import type { Metadata } from 'next';
import { StubPage } from '@/components/ui/StubPage';

export const metadata: Metadata = { title: 'Videoer' };
export default function VideoerPage() {
  return <StubPage title="Videoer" />;
}
