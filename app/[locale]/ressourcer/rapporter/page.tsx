import type { Metadata } from 'next';
import { StubPage } from '@/components/ui/StubPage';

export const metadata: Metadata = { title: 'Rapporter' };
export default function RapporterPage() {
  return <StubPage title="Rapporter" />;
}
