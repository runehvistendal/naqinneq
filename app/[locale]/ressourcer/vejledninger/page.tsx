import type { Metadata } from 'next';
import { StubPage } from '@/components/ui/StubPage';

export const metadata: Metadata = { title: 'Vejledninger' };
export default function VejledningerPage() {
  return <StubPage title="Vejledninger" />;
}
