import type { Metadata } from 'next';
import { StubPage } from '@/components/ui/StubPage';

export const metadata: Metadata = { title: 'Materialer' };
export default function MaterialerPage() {
  return <StubPage title="Materialer" />;
}
